// Listens for messages from the content script
chrome.runtime.onMessage.addListener(function (msg, sender) {
    // TODO: After validating the message's structure, we should initiate the page action to show (aka the icon to be active) on this current tab
});